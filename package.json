{
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend -- --outDir ../dist"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "axios": "^1.20.0",
    "multer": "^2.3.0",
    "tailwindcss": "^4.3.3"
  }
}
